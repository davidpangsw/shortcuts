# VSCode Keyboard Shortcuts

## Lab 1: Command Palette and Quick Open

1. Show Command Palette
2. Browse available commands
3. Close
4. Quick Open, Go to File
5. Type filename to search
6. Close

%

1. Show Command Palette (`⇧⌘P`)
2. Browse available commands
3. Close (`Esc`)
4. Quick Open, Go to File (`⌘P`)
5. Type filename to search
6. Close (`Esc`)

## Lab 2: Navigate to Symbol and Back

1. Go to Symbol
2. Type symbol name and navigate to it
3. Go back

%

1. Go to Symbol (`⇧⌘O`)
2. Type symbol name and navigate to it
3. Go back (`⌃-`)

## Lab 3: Navigate to Definition and Back

1. Place cursor on function call
2. Go to Definition
3. Review the implementation
4. Go back

%

1. Place cursor on function call
2. Go to Definition (`F12`)
3. Review the implementation
4. Go back (`⌃-`)

## Lab 4: Peek Definition Without Navigating

1. Place cursor on function name
2. Peek Definition
3. Review in peek window
4. Close

%

1. Place cursor on function name
2. Peek Definition (`⌥F12`)
3. Review in peek window
4. Close (`Esc`)

## Lab 5: Open Definition to Side

1. Place cursor on function call
2. Open Definition to the side
3. Review in side editor
4. Close side editor

%

1. Place cursor on function call
2. Open Definition to the side (`⌘K F12`)
3. Review in side editor
4. Close side editor (`⌘W`)

## Lab 6: Find All References

1. Place cursor on symbol
2. Show References
3. Review all references
4. Close

%

1. Place cursor on symbol
2. Show References (`⇧F12`)
3. Review all references
4. Close (`Esc`)

## Lab 7: Rename Symbol Safely

1. Place cursor on symbol
2. Rename Symbol
3. Type new name
4. Apply
5. Undo to restore

%

1. Place cursor on symbol
2. Rename Symbol (`F2`)
3. Type new name
4. Apply (`Enter`)
5. Undo to restore (`⌘Z`)

## Lab 8: Move Line Up and Down

1. Place cursor on a line
2. Move line down
3. Move line down
4. Move line up
5. Move line up

%

1. Place cursor on a line
2. Move line down (`⌥↓`)
3. Move line down (`⌥↓`)
4. Move line up (`⌥↑`)
5. Move line up (`⌥↑`)

## Lab 9: Copy Line Down and Up

1. Place cursor on a line
2. Copy line down
3. Delete the copied line

%

1. Place cursor on a line
2. Copy line down (`⇧⌥↓`)
3. Delete the copied line (`⇧⌘K`)

## Lab 10: Insert Line Below and Above

1. Place cursor anywhere in a line
2. Insert line below
3. Type some text
4. Delete line
5. Insert line above
6. Type some text
7. Delete line

%

1. Place cursor anywhere in a line
2. Insert line below (`⌘Enter`)
3. Type some text
4. Delete line (`⇧⌘K`)
5. Insert line above (`⇧⌘Enter`)
6. Type some text
7. Delete line (`⇧⌘K`)

## Lab 11: Indent and Outdent

1. Select lines to indent
2. Indent line
3. Indent line
4. Outdent line
5. Outdent line

%

1. Select lines to indent
2. Indent line (`⌘]`)
3. Indent line (`⌘]`)
4. Outdent line (`⌘[`)
5. Outdent line (`⌘[`)

## Lab 12: Jump to Matching Bracket

1. Place cursor at opening bracket
2. Jump to matching bracket
3. Jump to matching bracket

%

1. Place cursor at opening bracket
2. Jump to matching bracket (`⇧⌘\`)
3. Jump to matching bracket (`⇧⌘\`)

## Lab 13: Fold and Unfold Region

1. Place cursor in a function/class
2. Fold region
3. Review collapsed code
4. Unfold region

%

1. Place cursor in a function/class
2. Fold region (`⌥⌘[`)
3. Review collapsed code
4. Unfold region (`⌥⌘]`)

## Lab 14: Fold All and Unfold All Subregions

1. Place cursor in a nested region
2. Fold all subregions
3. Review structure
4. Unfold all subregions

%

1. Place cursor in a nested region
2. Fold all subregions (`⌘K ⌘[`)
3. Review structure
4. Unfold all subregions (`⌘K ⌘]`)

## Lab 15: Fold All and Unfold All Regions

1. Fold all regions
2. Review file structure
3. Unfold all regions

%

1. Fold all regions (`⌘K ⌘0`)
2. Review file structure
3. Unfold all regions (`⌘K ⌘J`)

## Lab 16: Comment and Uncomment Lines

1. Select lines to comment
2. Toggle line comment
3. Review commented code
4. Toggle line comment

%

1. Select lines to comment
2. Toggle line comment (`⌘/`)
3. Review commented code
4. Toggle line comment (`⌘/`)

## Lab 17: Add and Remove Line Comments

1. Select lines
2. Add line comment
3. Review
4. Remove line comment

%

1. Select lines
2. Add line comment (`⌘K ⌘C`)
3. Review
4. Remove line comment (`⌘K ⌘U`)

## Lab 18: Toggle Block Comment

1. Select code block
2. Toggle block comment
3. Review
4. Toggle block comment

%

1. Select code block
2. Toggle block comment (`⇧⌥A`)
3. Review
4. Toggle block comment (`⇧⌥A`)

## Lab 19: Toggle Word Wrap

1. Toggle word wrap
2. Observe wrapped lines
3. Toggle word wrap

%

1. Toggle word wrap (`⌥Z`)
2. Observe wrapped lines
3. Toggle word wrap (`⌥Z`)

## Lab 20: Multi-Cursor Editing

1. Place cursor at first location
2. Insert cursor above
3. Insert cursor below
4. Insert cursor below
5. Type changes
6. Undo to restore
7. Undo last cursor operation

%

1. Place cursor at first location
2. Insert cursor above (`⌥⌘↑`)
3. Insert cursor below (`⌥⌘↓`)
4. Insert cursor below (`⌥⌘↓`)
5. Type changes
6. Undo to restore (`⌘Z`)
7. Undo last cursor operation (`⌘U`)

## Lab 21: Insert Cursor at Line Ends

1. Select multiple lines
2. Insert cursor at end of each line selected
3. Type to edit all line ends
4. Undo to restore

%

1. Select multiple lines
2. Insert cursor at end of each line selected (`⇧⌥I`)
3. Type to edit all line ends
4. Undo to restore (`⌘Z`)

## Lab 22: Select Current Line

1. Place cursor on a line
2. Select current line
3. Extend selection
4. Click elsewhere to deselect

%

1. Place cursor on a line
2. Select current line (`⌘L`)
3. Extend selection (`⌘L` again)
4. Click elsewhere to deselect

## Lab 23: Select All Occurrences (Works When Find Widget Open)

1. Place cursor on word (or select text)
2. Select all occurrences
3. Type to replace all
4. Undo to restore

%

1. Place cursor on word (or select text)
2. Select all occurrences (`⇧⌘L` - works even when find widget is open)
3. Type to replace all
4. Undo to restore (`⌘Z`)

## Lab 24: Select All Occurrences (Alternative)

1. Place cursor on word (or select text)
2. Select all occurrences
3. Type to replace all
4. Undo to restore

%

1. Place cursor on word (or select text)
2. Select all occurrences (`⌘F2` - only works when editor has focus)
3. Type to replace all
4. Undo to restore (`⌘Z`)

## Lab 25: Add Selection to Next Find Match

1. Select text
2. Add selection to next Find match
3. Add selection to next Find match
4. Type to edit all selected
5. Undo to restore

%

1. Select text
2. Add selection to next Find match (`⌘D`)
3. Add selection to next Find match (`⌘D`)
4. Type to edit all selected
5. Undo to restore (`⌘Z`)

## Lab 26: Move Last Selection to Next Find Match

1. Select text
2. Add selection to next Find match
3. Move last selection to next Find match
4. Clear selections

%

1. Select text
2. Add selection to next Find match (`⌘D`)
3. Move last selection to next Find match (`⌘K ⌘D`)
4. Clear selections (`Esc`)

## Lab 27: Expand and Shrink Selection

1. Place cursor inside expression
2. Expand selection
3. Expand selection
4. Shrink selection
5. Shrink selection

%

1. Place cursor inside expression
2. Expand selection (`⌃⇧⌘→`)
3. Expand selection (`⌃⇧⌘→`)
4. Shrink selection (`⌃⇧⌘←`)
5. Shrink selection (`⌃⇧⌘←`)

## Lab 28: Column Box Selection

1. Place cursor at start position
2. Column selection down
3. Column selection down
4. Column selection right
5. Type to edit all lines
6. Undo to restore

%

1. Place cursor at start position
2. Column selection down (`⇧⌥⌘↓`)
3. Column selection down (`⇧⌥⌘↓`)
4. Column selection right (`⇧⌥⌘→`)
5. Type to edit all lines
6. Undo to restore (`⌘Z`)

## Lab 29: Find and Find Next

1. Find
2. Type search term
3. Find next
4. Find next
5. Find previous
6. Close

%

1. Find (`⌘F`)
2. Type search term
3. Find next (`⌘G`)
4. Find next (`⌘G`)
5. Find previous (`⇧⌘G`)
6. Close (`Esc`)

## Lab 30: Replace in File

1. Replace
2. Type search term
3. Type replacement
4. Replace one or all
5. Undo if needed
6. Close

%

1. Replace (`⌥⌘F`)
2. Type search term
3. Type replacement
4. Replace one or all
5. Undo if needed (`⌘Z`)
6. Close (`Esc`)

## Lab 31: Select All Find Matches

1. Find
2. Type search term
3. Select all occurrences of Find match
4. Type to replace all
5. Undo to restore
6. Close

%

1. Find (`⌘F`)
2. Type search term
3. Select all occurrences of Find match (`⌥Enter`)
4. Type to replace all
5. Undo to restore (`⌘Z`)
6. Close (`Esc`)

## Lab 32: Trigger Suggestion and Parameter Hints

1. Start typing a function/variable name
2. Trigger suggestion
3. Select or cancel
4. Type function with parameters
5. Trigger parameter hints
6. Close

%

1. Start typing a function/variable name
2. Trigger suggestion (`⌃Space` or `⌘I`)
3. Select or cancel (`Esc`)
4. Type function with parameters
5. Trigger parameter hints (`⇧⌘Space`)
6. Close (`Esc`)

## Lab 33: Format Document and Selection

1. Format document
2. Review changes
3. Undo if not satisfied
4. Select a code block
5. Format selection
6. Undo to restore

%

1. Format document (`⇧⌥F`)
2. Review changes
3. Undo if not satisfied (`⌘Z`)
4. Select a code block
5. Format selection (`⌘K ⌘F`)
6. Undo to restore (`⌘Z`)

## Lab 34: Quick Fix

1. Place cursor on issue
2. Quick Fix
3. Review suggested fixes
4. Apply or cancel
5. Undo if applied

%

1. Place cursor on issue
2. Quick Fix (`⌘.`)
3. Review suggested fixes
4. Apply or cancel (`Esc`)
5. Undo if applied (`⌘Z`)

## Lab 35: Trim Trailing Whitespace

1. Add trailing spaces to a line
2. Trim trailing whitespace
3. Undo to restore

%

1. Add trailing spaces to a line
2. Trim trailing whitespace (`⌘K ⌘X`)
3. Undo to restore (`⌘Z`)

## Lab 36: Change File Language

1. Change file language
2. Select a different language
3. Observe syntax changes
4. Change file language back
5. Select original language

%

1. Change file language (`⌘K M`)
2. Select a different language
3. Observe syntax changes
4. Change file language back (`⌘K M`)
5. Select original language

## Lab 37: Show All Symbols in Workspace

1. Show all Symbols
2. Type symbol name
3. Navigate to symbol
4. Go back

%

1. Show all Symbols (`⌘T`)
2. Type symbol name
3. Navigate to symbol
4. Go back (`⌃-`)

## Lab 38: Go to Line

1. Note current line number
2. Go to Line
3. Type a different line number
4. Navigate
5. Go to Line
6. Type original line number
7. Navigate

%

1. Note current line number
2. Go to Line (`⌃G`)
3. Type a different line number
4. Navigate (`Enter`)
5. Go to Line (`⌃G`)
6. Type original line number
7. Navigate (`Enter`)

## Lab 39: Navigate Between Errors

1. Show Problems panel
2. Go to next error
3. Go to next error
4. Go to previous error
5. Go to previous error
6. Close panel

%

1. Show Problems panel (`⇧⌘M`)
2. Go to next error (`F8`)
3. Go to next error (`F8`)
4. Go to previous error (`⇧F8`)
5. Go to previous error (`⇧F8`)
6. Close panel (`⌘W`)

## Lab 40: Navigate Editor Group History

1. Navigate editor group history
2. Select a file from list
3. Navigate editor group history
4. Return to original file

%

1. Navigate editor group history (`⌃⇧Tab`)
2. Select a file from list
3. Navigate editor group history (`⌃⇧Tab`)
4. Return to original file

## Lab 41: Go Back and Forward

1. Jump to a definition
2. Go back
3. Go forward
4. Go back

%

1. Jump to a definition (`F12`)
2. Go back (`⌃-`)
3. Go forward (`⌃⇧-`)
4. Go back (`⌃-`)

## Lab 42: Split Editor

1. Split editor
2. Focus into 2nd editor group
3. Open a different file
4. Close editor
5. Focus into 1st editor group

%

1. Split editor (`⌘\`)
2. Focus into 2nd editor group (`⌘2`)
3. Open a different file (`⌘P`)
4. Close editor (`⌘W`)
5. Focus into 1st editor group (`⌘1`)

## Lab 43: Focus Editor Groups

1. Split editor
2. Focus into 2nd editor group
3. Focus into 1st editor group
4. Focus into 3rd editor group
5. Close all but first

%

1. Split editor (`⌘\`)
2. Focus into 2nd editor group (`⌘2`)
3. Focus into 1st editor group (`⌘1`)
4. Focus into 3rd editor group (`⌘3`)
5. Close all but first (`⌘2` then `⌘W`)

## Lab 44: Move Editor Between Groups

1. Split editor
2. Move editor left
3. Move editor right
4. Close 2nd group

%

1. Split editor (`⌘\`)
2. Move editor left (`⌘K ⇧⌘←`)
3. Move editor right (`⌘K ⇧⌘→`)
4. Close 2nd group (`⌘2` then `⌘W`)

## Lab 45: Focus Into Previous/Next Editor Group

1. Split editor
2. Focus into next editor group
3. Focus into previous editor group
4. Close editor

%

1. Split editor (`⌘\`)
2. Focus into next editor group (`⌘K ⌘→`)
3. Focus into previous editor group (`⌘K ⌘←`)
4. Close editor (`⌘W`)

## Lab 46: Move Active Editor Group

1. Split editor
2. Move active editor group left
3. Move active editor group right
4. Close 2nd editor

%

1. Split editor (`⌘\`)
2. Move active editor group left (`⌘K ←`)
3. Move active editor group right (`⌘K →`)
4. Close 2nd editor (`⌘2` then `⌘W`)

## Lab 47: File Operations - New and Save

1. New File
2. Type some content
3. Save
4. Choose location and name
5. Close

%

1. New File (`⌘N`)
2. Type some content
3. Save (`⌘S`)
4. Choose location and name
5. Close (`⌘W`)

## Lab 48: Open and Close Files

1. Open File
2. Select a file
3. Review content
4. Close

%

1. Open File (`⌘O`)
2. Select a file
3. Review content
4. Close (`⌘W`)

## Lab 49: Save All

1. Modify multiple files
2. Save All
3. Verify all saved

%

1. Modify multiple files
2. Save All (`⌥⌘S`)
3. Verify all saved (no dots in tabs)

## Lab 50: Close All Editors

1. Open several files
2. Close All
3. Reopen closed editor

%

1. Open several files
2. Close All (`⌘K ⌘W`)
3. Reopen closed editor (`⇧⌘T`)

## Lab 51: Reopen Closed Editor

1. Note current file
2. Close
3. Reopen closed editor

%

1. Note current file
2. Close (`⌘W`)
3. Reopen closed editor (`⇧⌘T`)

## Lab 52: Navigate Open Files

1. Open next
2. Open next
3. Open previous
4. Open previous

%

1. Open next (`⌃Tab`)
2. Open next (`⌃Tab`)
3. Open previous (`⌃⇧Tab`)
4. Open previous (`⌃⇧Tab`)

## Lab 53: Copy and Reveal File Path

1. Copy path of active file
2. Paste somewhere to verify
3. Reveal active file in Finder
4. Return to VSCode

%

1. Copy path of active file (`⌘K P`)
2. Paste somewhere to verify
3. Reveal active file in Finder (`⌘K R`)
4. Return to VSCode

## Lab 54: Show Active File in New Window

1. Show active file in new window/instance
2. Review in new window
3. Close new window

%

1. Show active file in new window/instance (`⌘K O`)
2. Review in new window
3. Close new window (`⌘W`)

## Lab 55: Toggle Full Screen

1. Toggle full screen
2. Work in full screen
3. Toggle full screen

%

1. Toggle full screen (`⌃⌘F`)
2. Work in full screen
3. Toggle full screen (`⌃⌘F`)

## Lab 56: Toggle Editor Layout

1. Split editor
2. Toggle editor layout horizontal/vertical
3. Toggle editor layout back
4. Close 2nd editor

%

1. Split editor (`⌘\`)
2. Toggle editor layout horizontal/vertical (`⌥⌘0`)
3. Toggle editor layout back (`⌥⌘0`)
4. Close 2nd editor (`⌘2` then `⌘W`)

## Lab 57: Zoom In and Out

1. Zoom in
2. Zoom in
3. Zoom out
4. Zoom out

%

1. Zoom in (`⌘=`)
2. Zoom in (`⌘=`)
3. Zoom out (`⇧⌘-`)
4. Zoom out (`⇧⌘-`)

## Lab 58: Toggle Sidebar Visibility

1. Toggle Sidebar visibility
2. Work with full width
3. Toggle Sidebar visibility

%

1. Toggle Sidebar visibility (`⌘B`)
2. Work with full width
3. Toggle Sidebar visibility (`⌘B`)

## Lab 59: Navigate Sidebar Panels

1. Show Explorer
2. Show Search
3. Show Source Control
4. Show Debug
5. Show Extensions
6. Show Explorer

%

1. Show Explorer (`⇧⌘E`)
2. Show Search (`⇧⌘F`)
3. Show Source Control (`⌃⇧G`)
4. Show Debug (`⇧⌘D`)
5. Show Extensions (`⇧⌘X`)
6. Show Explorer (`⇧⌘E`)

## Lab 60: Search in Files Workflow

1. Show Search
2. Type search term
3. Review results
4. Click result to navigate
5. Go back
6. Show Explorer

%

1. Show Search (`⇧⌘F`)
2. Type search term
3. Review results
4. Click result to navigate
5. Go back (`⌃-`)
6. Show Explorer (`⇧⌘E`)

## Lab 61: Replace in Files

1. Show Search
2. Type search term
3. Toggle Search details
4. Type replacement text
5. Review and replace
6. Undo to restore
7. Show Explorer

%

1. Show Search (`⇧⌘F`)
2. Type search term
3. Toggle Search details (`⇧⌘J`)
4. Type replacement text
5. Review and replace
6. Undo to restore (`⌘Z`)
7. Show Explorer (`⇧⌘E`)

## Lab 62: Output Panel

1. Show Output panel
2. Review output
3. Close panel

%

1. Show Output panel (`⇧⌘U`)
2. Review output
3. Close panel (`⌘W`)

## Lab 63: Markdown Preview Workflow

1. Open Markdown preview to the side
2. Edit markdown
3. View live preview
4. Close preview

%

1. Open Markdown preview to the side (`⌘K V`)
2. Edit markdown
3. View live preview
4. Close preview (`⌘2` then `⌘W`)

## Lab 64: Markdown Preview Toggle

1. Open Markdown preview
2. View preview
3. Switch back to editor

%

1. Open Markdown preview (`⇧⌘V`)
2. View preview
3. Switch back to editor (`Esc`)

## Lab 65: Zen Mode

1. Zen Mode
2. Work in full screen, distraction-free
3. Exit

%

1. Zen Mode (`⌘K Z`)
2. Work in full screen, distraction-free
3. Exit (`Esc` `Esc`)

## Lab 66: Debug - Toggle Breakpoint

1. Place cursor on a line
2. Toggle breakpoint
3. Verify red dot appears
4. Toggle breakpoint

%

1. Place cursor on a line
2. Toggle breakpoint (`F9`)
3. Verify red dot appears
4. Toggle breakpoint (`F9`)

## Lab 67: Debug - Step Through Code

1. Toggle breakpoint
2. Start/Continue
3. Step over
4. Step into
5. Step out
6. Continue or Stop
7. Toggle breakpoint to remove

%

1. Toggle breakpoint (`F9`)
2. Start/Continue (`F5`)
3. Step over (`F10`)
4. Step into (`F11`)
5. Step out (`⇧F11`)
6. Continue or Stop (`F5` or `⇧F5`)
7. Toggle breakpoint to remove (`F9`)

## Lab 68: Show Hover

1. Place cursor on symbol
2. Show hover
3. Review information
4. Close

%

1. Place cursor on symbol
2. Show hover (`⌘K ⌘I`)
3. Review information
4. Close (`Esc` or click away)

## Lab 69: Integrated Terminal - Create and Toggle

1. Show integrated terminal
2. Run a command
3. Hide terminal
4. Show terminal
5. Create new terminal
6. Close terminals

%

1. Show integrated terminal (`` ⌃` ``)
2. Run a command
3. Hide terminal (`` ⌃` ``)
4. Show terminal (`` ⌃` ``)
5. Create new terminal (`` ⌃⇧` ``)
6. Close terminals (`⌘W`)

## Lab 70: Terminal - Navigate Output

1. Show integrated terminal
2. Run command with long output
3. Scroll up
4. Scroll down
5. Scroll to top
6. Scroll to bottom

%

1. Show integrated terminal (`` ⌃` ``)
2. Run command with long output
3. Scroll up (`⌘↑`)
4. Scroll down (`⌘↓`)
5. Scroll to top (`⌘Home`)
6. Scroll to bottom (`⌘End`)

## Lab 71: New Window and Close

1. New window/instance
2. Work in new window
3. Close window/instance

%

1. New window/instance (`⇧⌘N`)
2. Work in new window
3. Close window/instance (`⌘W`)

## Lab 72: User Settings

1. User Settings
2. Search for a setting
3. Review or modify
4. Close Settings

%

1. User Settings (`⌘,`)
2. Search for a setting
3. Review or modify
4. Close Settings (`⌘W`)

## Lab 73: Keyboard Shortcuts

1. Keyboard Shortcuts
2. Search for a command
3. Review shortcut
4. Close

%

1. Keyboard Shortcuts (`⌘K ⌘S`)
2. Search for a command
3. Review shortcut
4. Close (`⌘W`)

## Lab 74: Cut and Copy Empty Selection

1. Place cursor on line (no selection)
2. Cut line
3. Move to different location
4. Paste
5. Undo to restore
6. Place cursor on line
7. Copy line
8. Move to different location
9. Insert line below
10. Paste
11. Undo to restore

%

1. Place cursor on line (no selection)
2. Cut line (`⌘X`)
3. Move to different location
4. Paste (`⌘V`)
5. Undo to restore (`⌘Z`)
6. Place cursor on line
7. Copy line (`⌘C`)
8. Move to different location
9. Insert line below (`⌘Enter`)
10. Paste (`⌘V`)
11. Undo to restore (`⌘Z`)

## Lab 75: Delete Line

1. Place cursor on line
2. Copy line to preserve
3. Delete line
4. Undo to restore

%

1. Place cursor on line
2. Copy line to preserve (`⌘C`)
3. Delete line (`⇧⌘K`)
4. Undo to restore (`⌘Z`)

## Lab 76: Navigate to Beginning/End of Line

1. Place cursor mid-line
2. Go to end of line
3. Go to beginning of line

%

1. Place cursor mid-line
2. Go to end of line (`End`)
3. Go to beginning of line (`Home`)

## Lab 77: Navigate to Beginning/End of File

1. Go to end of file
2. Go to beginning of file

%

1. Go to end of file (`⌘↓`)
2. Go to beginning of file (`⌘↑`)

## Lab 78: Scroll Line Up/Down

1. Note cursor position
2. Scroll line down
3. Scroll line down
4. Scroll line up
5. Scroll line up

%

1. Note cursor position
2. Scroll line down (`⌃PgDn`)
3. Scroll line down (`⌃PgDn`)
4. Scroll line up (`⌃PgUp`)
5. Scroll line up (`⌃PgUp`)

## Lab 79: Scroll Page Up/Down

1. Scroll page down
2. Scroll page down
3. Scroll page up
4. Scroll page up

%

1. Scroll page down (`⌘PgDn`)
2. Scroll page down (`⌘PgDn`)
3. Scroll page up (`⌘PgUp`)
4. Scroll page up (`⌘PgUp`)

## Lab 80: Keep Preview Mode Editor Open

1. Single-click file in Explorer (opens in preview)
2. Note italic tab name (preview mode)
3. Keep preview mode editor open
4. Note regular tab name (persisted)
5. Close file

%

1. Single-click file in Explorer (opens in preview)
2. Note italic tab name (preview mode)
3. Keep preview mode editor open (`⌘K Enter`)
4. Note regular tab name (persisted)
5. Close file (`⌘W`)

## Lab 81: Close Folder

1. Close folder
2. Confirm if prompted
3. Reopen folder

%

1. Close folder (`⌘K F`)
2. Confirm if prompted
3. Reopen folder (`⌘O`)

## Lab 82: Toggle Tab Moves Focus

1. Toggle Tab moves focus
2. Tab (moves focus instead of indent)
3. Toggle Tab moves focus
4. Tab (normal indent behavior)

%

1. Toggle Tab moves focus (`⌃⇧M`)
2. Tab (`Tab` moves focus instead of indent)
3. Toggle Tab moves focus (`⌃⇧M`)
4. Tab (`Tab` normal indent behavior)

## Lab 83: Column Selection Page Navigation

1. Place cursor at column start
2. Column selection page down
3. Review selection
4. Deselect
5. Column selection page up
6. Deselect

%

1. Place cursor at column start
2. Column selection page down (`⇧⌥⌘PgDn`)
3. Review selection
4. Deselect (`Esc`)
5. Column selection page up (`⇧⌥⌘PgUp`)
6. Deselect (`Esc`)

## Lab 84: Workflow - Quick File Edit

1. Quick Open
2. Type filename
3. Open
4. Make edit
5. Save
6. Close

%

1. Quick Open (`⌘P`)
2. Type filename
3. Open (`Enter`)
4. Make edit
5. Save (`⌘S`)
6. Close (`⌘W`)

## Lab 85: Workflow - Multi-File Search and Edit

1. Show Search
2. Search for term
3. Navigate to result
4. Make changes
5. Go back
6. Show Explorer

%

1. Show Search (`⇧⌘F`)
2. Search for term
3. Navigate to result
4. Make changes
5. Go back (`⌃-`)
6. Show Explorer (`⇧⌘E`)

## Lab 86: Workflow - Refactor with References

1. Place cursor on symbol
2. Show References
3. Review all usages
4. Close
5. Rename Symbol
6. Type new name
7. Apply
8. Undo to restore

%

1. Place cursor on symbol
2. Show References (`⇧F12`)
3. Review all usages
4. Close (`Esc`)
5. Rename Symbol (`F2`)
6. Type new name
7. Apply (`Enter`)
8. Undo to restore (`⌘Z`)
